# 更新记录

## 0.1.16

- GitLab 首页改为项目台账，上传、下载与历史记录的入口和状态更清楚。
- 上传向导会保留未完成草稿和失败后的本地提交，支持返回后继续上传。
- 交付包页面明确说明只打包 `output` 下的第二级子文件夹。
- 账户、历史、错误和更新页面统一布局，并修复跨页面时公共按钮样式丢失。
- 加固登录证书、加密凭据、外部链接和 `charge.json` 失败恢复处理。
- MR 附件上传前检查项目可见范围，公开项目禁止上传附件，失败重试不会重复上传。

## 0.1.15

- 更新页面只显示用户实际跨越版本的说明，不再把整份历史记录塞进单个版本。
- 正确清理 GitHub 返回的 HTML 标签，更新内容恢复为简洁的中文列表。
- 发布工具自动提取当前版本内容，防止后续版本再次混入旧记录。

## 0.1.14

- 正式安装版启动约 30 秒后自动检查一次软件更新。
- 软件持续运行时每 6 小时再次检查，发现新版本后发送系统通知。
- 定期检查只读取版本信息，不会自动下载、自动安装或打断工程操作。

## 0.1.13

- 修复 GitLab 项目外链错误地拼入 SSH 克隆地址并打开 404 页的问题。
- SSH、HTTP 和 HTTPS 仓库地址现在统一转换为正确的 GitLab 项目网页路径。
- 上传完成后的 GitLab 提交链接同步使用相同的地址转换规则。

## 0.1.12

- 下载新项目后立即刷新项目列表，不再需要通过“找到本机已有项目”重复添加。
- `charge.json` 支持 `output` 下任意分类目录，并在上传页面明确展示生成位置和完整交付路径。
- 全新项目只有 `main` 时，可从 `main` 创建首个 `dev/` 工作版本；`main` 仍禁止直接上传。
- 点击“返回项目操作”会主动清空本次上传草稿，切换其他页面则继续保留。
- 项目操作页增加“在 GitLab 查看本项目”，通过系统浏览器打开对应项目页面。

## 0.1.11

- 提交合并审核时可为 T0、T1、T2 定版、打样或设变等关键节点建立版本标记（Tag）。
- 软件严格按“先在当前开发分支建立并推送 Tag，再创建 MR”的顺序执行。
- 已有 Tag 指向同一提交时可安全重试 MR；指向其他提交时停止并提示改名，不会移动旧 Tag。
- 项目操作记录分别标注分支提交、版本 Tag 和已合并审核，并区分开发分支与 `main`。
- GitLab 项目首页增加“刷新项目概况”，只重新读取项目状态，不修改或上传工程文件。

## 0.1.10

- 离开 GitLab 页面后，会记住当前项目、操作步骤和已经填写的内容；重新打开软件也可以继续。
- 上传失败但本地更新已经保存时，会显示“继续上传”，不要求重新选择文件或再次创建更新。
- 项目历史会读取全部云端分支，支持手动刷新，并显示每条记录所属的工作版本。
- 通过 Trae、命令行等其他工具完成上传后，只要当前分支已经与云端同步且 `charge.json` 有交付内容，仍可继续创建合并审核。
- 本地和云端同时存在新内容时，会停止直接上传并给出安全处理提示，避免覆盖任何一边。
- 合并审核状态会在离开页面或重启软件后继续跟踪。
- 下载新版本时会立即显示进度、后台状态和完成或失败通知。
- 安装程序会提示大文件解压可能需要数分钟，避免将短暂停留误认为卡死。
